# Add the actual key only to .env.local. Never commit it.
OPENROUTER_API_KEY=

# Convex creates this after the one-time development login.
NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=

# Add these after the private Vercel preview URL is stable.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
